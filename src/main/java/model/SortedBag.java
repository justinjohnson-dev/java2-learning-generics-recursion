package model;

import interfaces.IBag;
import java.util.ArrayList;

public class SortedBag implements IBag {

    private ArrayList<Bag> data = new ArrayList<>();

    // null values for now
    public void add(E item) {

    }
    public E remove() {
        return E;
    }
    public boolean contains(E item) {
        return true;
    }
    public boolean empty() {
        return true;
    }
}
